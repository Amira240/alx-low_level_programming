#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints a text according number
 * Return: Always (success)
 *
 */

int main(void)
{         char low;
for (low = 'z'; low >= 'a'; low--)
putchar(low);
putchar('\n');
return (0);
}
